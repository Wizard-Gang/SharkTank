# v1.0.1

Allows only GitHub-verified Dependabot dependency commits to bypass the human ST subject/body template, updates the official checkout and Node setup actions to v7, and generalizes sequential provenance validation.

This patch fixes dependency-automation pull requests discovered immediately after public repository publication. Production deployment remains disabled by default.

Tag target: ST-032.
