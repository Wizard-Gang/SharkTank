# v1.0.0

Complete self-contained public repository: all committed product code, structured provenance, current build tooling, unit and cross-runtime tests, least-privilege CI, contributor/security controls, canonical documentation, immutable releases, and exact-tag deployment identity.

Production deployment remains disabled by default. Publishing this release does not cut over production, change DNS, enable the deployment variable, or retire a legacy repository.

Tag target: ST-031.
