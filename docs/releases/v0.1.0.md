# v0.1.0

Buildable project foundation, portable deterministic game source, React client, Worker host, blob API, and local loop. The private game gitlink is represented as ordinary files.

Known limitations: no authoritative realtime rooms, PHP parity runtime, production controls, public assurance register, or repository CI.

Tag target: ST-003.
