# v1.0.2

Records the successful deployment of v1.0.1 from the canonical public repository and the completed production-parity checks: public evidence, APIs, security boundaries, release identity, real WebSocket admission, and WizardGang portfolio links.

This is a controlled-record release. It does not change the runtime deployed as v1.0.1. It closes the gate for archiving `WizardGangLocal` while retaining that repository and its history as immutable provenance.

Tag target: ST-033.
