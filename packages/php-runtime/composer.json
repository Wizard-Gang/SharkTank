{
  "name": "wizardgang/module-php",
  "description": "PHP proof-of-concept backend for the Snake Arena game — 1:1 protocol parity with the TypeScript/Cloudflare backend. Same wire format, same deterministic engine, same ops pages.",
  "type": "project",
  "require": {
    "php": ">=8.1",
    "workerman/workerman": "^4.1"
  },
  "autoload": {
    "psr-4": { "ModulePHP\\": "src/" }
  },
  "scripts": {
    "start": "php start.php start",
    "selftest": "php bin/selftest.php"
  }
}
